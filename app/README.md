# App Routes (Phase Migration)

This project currently uses React Navigation (`src/navigation`).

The `app/` directory is introduced to align with the AQ-Cloud folder pattern.
A future phase can migrate screens into route-like modules if desired.

